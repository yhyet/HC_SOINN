# KAC分类器集成到LAMDA-PILOT_2项目工作计划书

## 项目目标
将KAC (Kolmogorov-Arnold Classifier) 分类器集成到LAMDA-PILOT_2项目中，参考hc-soinn分类器的实现形式，实现一个可训练的KAC分类器，并嵌入到codaprompt项目中，同时保留扩展到其他项目的能力。

## 参考实现分析

### HC-SOINN分类器的实现模式
1. **独立文件**: `utils/hc_soinn_classifier.py` - 独立的分类器实现
2. **集成方式**: 在 `models/coda_prompt.py` 中通过参数 `use_hc_soinn` 控制启用
3. **训练流程**:
   - 初始化: 在 `__init__` 中根据参数初始化分类器
   - 构建: 在 `_build_hc_soinn_bank()` 中构建分类器（添加特征、压缩）
   - 评估: 在 `_eval_hc_soinn()` 中使用分类器进行评估
   - 任务后处理: 在 `after_task()` 中处理任务结束后的操作

### 当前FC层的位置
- `utils/inc_net.py` 中的 `CodaPromptVitNet` 类
- `self.fc = nn.Linear(768, args["nb_classes"])` - 标准线性分类器
- 训练时只更新 `prompt` 和 `fc` 的参数

## 具体任务清单

### 阶段一：创建KAC分类器独立模块

#### 任务1.1: 创建 `utils/kac_classifier.py`
**目标**: 创建独立的KAC分类器实现文件

**具体内容**:
- [ ] 从 `KAC/models/kac_clf.py` 移植核心组件：
  - `SplineLinear` 类
  - `RadialBasisFunction` 类  
  - `KACLayer` 类
- [ ] 实现KAC分类器主类 `KACClassifier`，参考 `HCSOINNClassifier` 的接口设计：
  - `__init__()`: 初始化参数（grid_min, grid_max, num_grids等）
  - `forward()`: 前向传播，输入特征 [B, 768]，输出logits [B, num_classes]
  - `update()`: 更新分类器（用于增量学习场景，扩展类别数）
  - `to(device)`: 设备迁移
  - `state_dict()` / `load_state_dict()`: 模型保存/加载
- [ ] 添加必要的工具函数和文档注释
- [ ] 确保代码风格与项目一致

**输出**: `utils/kac_classifier.py` 文件

---

### 阶段二：修改网络结构以支持KAC分类器

#### 任务2.1: 修改 `utils/inc_net.py` 中的 `CodaPromptVitNet`
**目标**: 在 `CodaPromptVitNet` 中添加KAC分类器支持

**具体内容**:
- [ ] 添加参数 `use_kac` 和 `kac_config` 到 `__init__` 方法
- [ ] 修改 `generate_fc()` 方法：
  - 当 `use_kac=True` 时，使用 `KACLayer` 替代 `nn.Linear`
  - 当 `use_kac=False` 时，保持原有的 `nn.Linear` 实现
- [ ] 修改 `update_fc()` 方法：
  - 支持KAC分类器的增量更新（扩展输出维度）
  - 保持与线性分类器相同的接口
- [ ] 确保 `forward()` 方法兼容KAC分类器（KACLayer的forward接口与Linear相同）
- [ ] 添加 `extract_vector()` 方法（如果不存在），用于特征提取

**输出**: 修改后的 `utils/inc_net.py`

---

### 阶段三：修改训练流程以支持KAC分类器训练

#### 任务3.1: 修改 `models/coda_prompt.py` 中的初始化
**目标**: 添加KAC分类器的初始化逻辑

**具体内容**:
- [ ] 在 `__init__` 方法中添加参数解析：
  - `use_kac = args.get("use_kac", False)`
  - `kac_config = args.get("kac_config", {})` (包含grid_min, grid_max, num_grids等)
- [ ] 在 `CodaPromptVitNet` 初始化时传递KAC相关参数
- [ ] 添加日志输出，显示KAC分类器的配置信息

**输出**: 修改后的 `models/coda_prompt.py` 的 `__init__` 部分

#### 任务3.2: 修改优化器设置
**目标**: 确保KAC分类器参数参与训练

**具体内容**:
- [ ] 修改 `get_optimizer()` 方法：
  - 检查 `self._network.fc` 是否为KAC分类器
  - 如果是KAC，将KAC的参数添加到优化器参数列表中
  - 确保KAC分类器的所有可训练参数都被包含
- [ ] 验证参数数量统计（与现有日志保持一致）

**输出**: 修改后的 `get_optimizer()` 方法

#### 任务3.3: 修改训练循环
**目标**: 确保KAC分类器在训练时正常工作

**具体内容**:
- [ ] 检查 `_init_train()` 方法：
  - 确保KAC分类器在训练模式下正常工作
  - 验证前向传播和反向传播流程
- [ ] 检查 `_train()` 方法中的损失计算：
  - KAC分类器的输出格式应与线性分类器兼容
  - 确保logits维度正确
- [ ] 添加KAC分类器特定的训练监控（可选）

**输出**: 验证训练流程正常工作

---

### 阶段四：修改评估流程

#### 任务4.1: 添加KAC分类器评估方法
**目标**: 实现KAC分类器的评估逻辑

**具体内容**:
- [ ] 在 `models/coda_prompt.py` 中添加 `_eval_kac()` 方法：
  - 参考 `_eval_fc()` 的实现
  - 使用KAC分类器进行前向传播
  - 返回预测结果和真实标签
- [ ] 在 `eval_task()` 方法中添加KAC分类器的评估分支：
  - 当 `use_kac=True` 时，调用 `_eval_kac()`
  - 将结果添加到 `results` 字典中，键为 `"kac"`

**输出**: 修改后的评估方法

---

### 阶段五：配置文件支持

#### 任务5.1: 创建KAC配置文件示例
**目标**: 创建使用KAC分类器的实验配置文件

**具体内容**:
- [ ] 在 `exps/` 目录下创建示例配置文件：
  - `coda_prompt_kac_cifar224.json`
  - `coda_prompt_kac_cub.json`
  - `coda_prompt_kac_imagenetr.json`
- [ ] 配置文件中包含：
  - `"use_kac": true`
  - `"kac_config"`: 包含grid_min, grid_max, num_grids等参数
  - 其他必要的训练参数
- [ ] 参考现有的 `coda_prompt_hc_soinn_*.json` 配置文件格式

**输出**: 3个示例配置文件

---

### 阶段六：测试与验证

#### 任务6.1: 单元测试
**目标**: 验证KAC分类器的基本功能

**具体内容**:
- [ ] 测试 `KACClassifier` 的初始化
- [ ] 测试前向传播（输入输出维度）
- [ ] 测试参数更新（增量学习场景）
- [ ] 测试设备迁移（CPU/GPU）
- [ ] 测试模型保存/加载

**输出**: 测试脚本和测试报告

#### 任务6.2: 集成测试
**目标**: 验证KAC分类器在codaprompt中的完整流程

**具体内容**:
- [ ] 在小规模数据集上运行完整训练流程
- [ ] 验证训练损失收敛
- [ ] 验证评估结果正确性
- [ ] 对比KAC分类器与线性分类器的性能
- [ ] 检查内存使用情况

**输出**: 集成测试报告

---

### 阶段七：文档与扩展性

#### 任务7.1: 代码文档
**目标**: 添加详细的代码注释和文档

**具体内容**:
- [ ] 为 `KACClassifier` 类添加详细的docstring
- [ ] 为关键方法添加注释
- [ ] 添加使用示例
- [ ] 添加参数说明

**输出**: 完善的代码文档

#### 任务7.2: 扩展性设计
**目标**: 确保KAC分类器可以轻松扩展到其他项目

**具体内容**:
- [ ] 确保 `KACClassifier` 接口通用，不依赖特定项目结构
- [ ] 提供清晰的集成指南（README或文档）
- [ ] 确保参数配置灵活，易于调整
- [ ] 考虑向后兼容性（不影响现有功能）

**输出**: 扩展性设计文档

---

## 关键技术点

### 1. KAC分类器与线性分类器的接口兼容性
- **问题**: 确保KACLayer的forward接口与nn.Linear兼容
- **解决方案**: 
  - KACLayer的forward方法应接受 [B, D] 输入，返回 [B, num_classes] 输出
  - 与nn.Linear保持相同的调用方式

### 2. 增量学习场景下的类别扩展
- **问题**: 当新任务到来时，需要扩展KAC分类器的输出维度
- **解决方案**:
  - 实现 `update_fc()` 方法，支持动态扩展输出维度
  - 新类别的权重可以随机初始化或使用特定策略

### 3. 训练时的参数更新
- **问题**: 确保KAC分类器的所有可训练参数都参与优化
- **解决方案**:
  - 在 `get_optimizer()` 中正确收集KAC分类器的参数
  - 验证参数是否被正确添加到优化器

### 4. 与现有分类器的共存
- **问题**: KAC分类器应该与NCM、KNN等分类器可以共存
- **解决方案**:
  - KAC分类器作为FC层的替代，不影响其他分类器的使用
  - 在评估时，KAC分类器作为独立的评估分支

---

## 实施顺序建议

1. **阶段一** → 创建独立模块（基础）
2. **阶段二** → 修改网络结构（核心集成）
3. **阶段三** → 修改训练流程（功能实现）
4. **阶段四** → 修改评估流程（验证功能）
5. **阶段五** → 配置文件（便于使用）
6. **阶段六** → 测试验证（质量保证）
7. **阶段七** → 文档完善（可维护性）

---

## 风险评估与应对

### 风险1: KAC分类器训练不稳定
- **应对**: 
  - 参考KAC项目的训练配置
  - 添加梯度裁剪和学习率调整
  - 进行小规模实验验证

### 风险2: 内存占用增加
- **应对**:
  - KAC分类器参数数量约为线性分类器的16倍（num_grids=16）
  - 监控内存使用，必要时调整num_grids参数

### 风险3: 性能下降
- **应对**:
  - 对比KAC与线性分类器的性能
  - 如果性能不理想，分析原因并优化

### 风险4: 与现有代码冲突
- **应对**:
  - 通过参数开关控制，默认不启用KAC
  - 确保向后兼容性

---

## 成功标准

1. ✅ KAC分类器可以成功训练
2. ✅ 训练损失正常收敛
3. ✅ 评估结果正确
4. ✅ 可以与线性分类器切换使用
5. ✅ 代码结构清晰，易于维护
6. ✅ 可以扩展到其他项目

---

## 时间估算

- 阶段一: 2-3小时
- 阶段二: 2-3小时
- 阶段三: 3-4小时
- 阶段四: 1-2小时
- 阶段五: 1小时
- 阶段六: 3-4小时
- 阶段七: 2小时

**总计**: 14-19小时

---

## 备注

- 本计划书基于对现有代码的分析，实际实施时可能需要根据具体情况调整
- 建议先完成阶段一和阶段二，验证基本功能后再继续后续阶段
- 保持与现有代码风格的一致性
- 注意代码的可读性和可维护性






