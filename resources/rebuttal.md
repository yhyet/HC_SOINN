**尊敬的审稿人(Reviewer y6R5),**

*(温馨提示：为方便查阅，我们在本回复中涉及的图表已统一放置于匿名链接中：[https://anonymous.4open.science/r/ICML2026-Rebuttal-D2D6/](https://www.google.com/search?q=https://anonymous.4open.science/r/ICML2026-Rebuttal-D2D6/) 。)*

我们衷心感谢您提出的建设性反馈以及“Weak Accept”的推荐，也非常感谢您对我们拓扑方法动机和广泛泛化能力的认可。针对您在推理效率、内存开销以及超参数调优方面的担忧，我们进行了深入的优化与详细的澄清，具体回复如下：

**针对缺点 1 的回复：推理延迟与内存消耗**

**1. 推理延迟的深度优化：**
我们非常感谢您对模型效率的严谨审查。我们完全承认最初的实现中确实存在推理延迟较高的问题。在您的宝贵建议启发下，我们对推理流程进行了重新设计，引入了“两阶段由粗到细检索 (Two-Stage Coarse-to-Fine Retrieval)”机制，从而大幅降低了计算开销：

- 我们不再全局计算所有 $C \times K$ 个子原型的距离，而是首先利用 NCM（类均值）距离对所有类别进行排序。这一步仅需进行一次简单的 $\mathbb{R}^{N \times C}$ 矩阵乘法。
- 接着，我们仅保留每个样本的前 $k_c$ 个候选类别，并只针对这些候选类计算子簇距离。当 $k_c \ll C$ 时，占主导地位的 $\mathbb{R}^{N \times M}$ 计算量按约 $C/k_c$ 的比例大幅下降。
- 此外，我们还在代码工程层面进行了全面优化，剔除了 PyTorch 实现中不必要的 CPU-GPU 内存往返拷贝以及 Query 的重复归一化操作。

如匿名链接中的 **Table-1** 所示（基于 Split CIFAR-100 和 CODA-Prompt 方法），我们优化后的实现在两种硬件平台上均实现了推理延迟的显著下降。虽然 HC-SOINN 相比于极简的 NCM 基线仍有少许延迟增加，但它比同样作为非线性分类器的 KAC（CVPR 2025）要更快，同时还提供了更高的分类精度。我们认为，为了换取显著的性能提升，这一适度的延迟权衡是可以接受的。

**2. 内存消耗的澄清：**
关于您对资源受限边缘设备上内存开销的担忧，为避免回复内容的冗余，**请您直接参考我们对审稿人 wzjD 的问题二（“明确代表性旧样本的存储预算与公平比较”）的详细回复**。

**针对缺点 2 的回复：超参数与实际可用性**

我们完全理解您的顾虑：引入额外的超参数可能会增加模型在现实动态数据流中部署的难度。但我们希望在此郑重澄清，**HC-SOINN 在面对不同任务和数据集时，实际上是“免调参（tuning-free）”的。**

- **跨场景的一致性配置：** 在论文评估的三大基准测试（涵盖粗粒度、细粒度及严重领域偏移）以及所有基线方法中，我们自始至终使用的是**完全相同的一套固定超参数**（即 $\alpha=0.5$, $K_{init}=60$, $age_{max}=20$, $T_{soinn}=1$）。此外，正式实验中 STAR 模块的动量参数 $\lambda$ 也统一设置为 **0.99**（非常抱歉我们在附录中遗漏了这一数值，我们将在修订版中补全）。我们自始至终未针对任何新任务进行过耗时的网格搜索。
- **高度鲁棒的参数空间：** 为了更直观地展示参数的鲁棒性，请您参考匿名链接中补充的这 5 个超参数的全面敏感性分析图。其中，**Fig 1.1 - 1.5** 展示了基于 CODA-Prompt (CIFAR-100) 的实验结果，**Fig 2.1 - 2.5** 展示了基于 DualPrompt/SimpleCIL (ImageNet-R) 的实验结果。大量的折线图清晰地表明，模型的性能在一个极其宽泛的参数区间内都保持着高度的稳定性。
- **统一参数旨在保障实际可用性：** 从补充的图中不难看出，如果针对特定数据集进行微调，模型的性能甚至会高于我们在论文中的汇报值（例如，在 ImageNet-R 的 SimpleCIL + HC-SOINN 实验中，若将 $\alpha$ 调整为 0.6，模型 $A_{Avg}$ 可达 70.43%，这显著高于我们在统一参数 $\alpha=0.5$ 下所汇报的 69.97%）。然而，**我们坚持在所有实验中报告使用统一参数的结果，正是为了保障并证明该方法在实际部署中的极佳可用性。** 在面对现实中不断变化的动态数据流时，重新调参是极不切实际的。我们的方法能够仅凭一套固定配置胜任各种复杂场景，是因为拓扑机制完全依赖于数据驱动的自组织生长。这种对复杂流形的动态刻画天然具备极强的适应性，从而彻底免去了人工对个别参数精雕细琢的负担。

为了打消读者的疑虑，我们将在修订版的附录中补充上述全面的敏感性分析图，并在正文中明确声明这种跨数据集的超参数稳定性与部署便利性。我们希望这些深度的优化和澄清能够打消您的顾虑，并进一步向您展示我们框架的实用价值。

**尊敬的审稿人(Reviewer wzjD),**

*(温馨提示：为方便查阅，我们在本回复中涉及的图表已统一放置于匿名链接中：[https://anonymous.4open.science/r/ICML2026-Rebuttal-D2D6/](https://www.google.com/search?q=https://anonymous.4open.science/r/ICML2026-Rebuttal-D2D6/) 。)*

我们衷心感谢您对我们工作的认可，感谢您给出的“Weak Accept”推荐，以及对我们问题动机、方法原创性和实验全面性的高度评价。针对您在理论分析、存储预算以及推理延迟方面提出的关键问题，我们进行了深入的补充分析与优化，具体回复如下：

**对问题 1 的回复：理论分析与“改进分类/减少遗忘”主张的对齐**

我们非常感谢您极其专业的理论洞察。您指出目前的分析主要限制了漂移下的表示误差，我们同意需要进一步将这一误差边界与最终的“分类性能提升”和“遗忘减少”建立更直接的联系。HC-SOINN 之所以能有效改进分类并抵抗遗忘，其内在逻辑体现在“任务内（Intra-task）的局部流形拟合”与“时间维度（Inter-task）上的抗非线性漂移”这两个维度。而我们论文中的理论证明（Proposition 1）主要是为了确立并证明其在时间维度上卓越的抗遗忘能力。具体而言：

1. **任务内（Intra-task）的流形拟合能力：**多原型的 HC-SOINN 天然比单原型更能精确覆盖和代表复杂的流形结构。这直接体现在，当每个 Task 引入新类时，在相同的骨干网络下，使用 HC-SOINN 分类器得到的新类初始精度在绝大多数情况下都显著优于单原型的 NCM 分类器。
2. **时间维度（Inter-task）的抗遗忘能力（理论证明的核心目标）：** 在 CIL 中，灾难性遗忘的本质原因是：随着新任务的更新，旧类的特征发生漂移，导致原有的决策边界被非线性扭曲。根据我们的 **Proposition 1**，HC-SOINN 的漂移误差上界严格小于 NCM ($\mathcal{E}*{HC} \le L\cdot r*{local} \ll L\cdot R_{c}$)。我们的证明正是为了建立这样一个直接联系：**表示层面偏离原始分布的程度越小，意味着决策边界受到的非线性破坏就越小。** 单一原型的偏移会导致整个类别的决策半空间发生剧烈偏转，而多拓扑节点能够将全局的扭曲分散为局部的微调，从而在不重新训练旧类样本的情况下，最大限度地维持旧类特征与分类器之间的对齐。因此，表示误差的上界约束，正是旧类决策边界稳定性的理论保证。我们将在修订版的理论部分明确补充这一连接逻辑。

**对问题 2 的回复：明确代表性旧样本的存储预算与公平比较**

感谢您指出这一点。我们需要明确澄清：**是的，在 STAR 模块中，每一个拓扑节点（Node）精确对应一个存储的真实历史锚点样本（Anchor Sample）。**

关于存储预算的具体数量与公平比较，实际上我们在论文的 **附录 D (表 5)** 和 **附录 E (表 6)** 中已经提供了详尽的实验分析，同时为了进一步展示预算的灵活性，我们在匿名链接中补充了新的验证实验（**请参考匿名链接中的 Table-2 以及 Fig 1.4, Fig 2.4**）。具体如下：

1. **确切的存储预算 (参考附录 D 表 5)：** 我们在附录 D 中详细分析了不同方法结合 HC-SOINN 后的拓扑节点数量。由于 STAR 存储的锚点与节点严格一一对应，该表即代表了我们的存储预算。数据显示，在 Split CIFAR-100 和 Split CUB-200 数据集上，所有基线方法的平均每类存储样本数均严格低于 20 个（约在 17~19 个之间）；仅在类内方差极大的 Split ImageNet-R 数据集上，部分方法（如 DualPrompt、CODA-Prompt 等）的平均存储样本才略微超过 20 个（整体平均为 20.61 个）。
2. **与传统方法的公平比较 (参考附录 E 表 6)：** 为了直观评估内存效率，我们在附录 E 中将该预算与经典的基于回放（Rehearsal）的方法（如 iCaRL, DER, FOSTER，标准设置均为每类严格存储 20 个样本）进行了直接对比。结果表明，我们的方法在处于**完全相同甚至更低**的内存预算水平（例如 CIFAR-100 上平均 18.48 个样本/类）下，在准确率上实现了对最强基线的显著超越。

此外，我们希望强调，STAR 方法对存储样本的需求是**高度灵活和弹性**的。在实际部署中，可以通过调整初始聚类目标数量 $K_{init}$ 或拓扑细化轮数 $T_{soinn}$ 来动态调整 STAR 的 Buffer 大小。存储样本的数量与分类精度呈现平滑的正相关性（这一点可以从匿名链接中的 **Fig 1.4 与 Fig 2.4** 中直观看出）。

**为了进一步证明这种灵活性对内存预算的严格可控性**，针对 Split ImageNet-R 数据集上部分方法节点均值略超 20 的情况，我们进行了补充验证（**详见匿名链接 Table-2**）。我们仅仅将拓扑细化轮数 $T_{soinn}$ 从 1 调整为 2，便成功使这些方法的节点均值严格降至 20 以下（例如 DualPrompt 的平均节点数从 21.38 显著降至 19.47）。值得注意的是，这种对存储预算的进一步压缩几乎没有带来性能损失，甚至在部分指标上还有所提升（例如 DualPrompt 的 $A_{Last}$ 从 69.75% 提升至 69.80%）。这充分证明了我们的模型可以根据实际边缘设备的严格限制，非常灵活地将存储预算控制在特定阈值以内，同时保持卓越的分类性能。我们将在正文中进一步提炼这段关于存储预算和弹性的明确对比与讨论。

**对问题 3 的回复：推理成本与部署加速**

我们非常重视您对实际部署中推理成本增加的担忧。为了避免回复内容的冗余，关于推理延迟的深度优化以及工程加速的具体细节，**请您直接参考我们对审稿人 y6R5 的“针对缺点 1 的回复：推理延迟与内存消耗”中的“1. 推理延迟的深度优化”部分的详细回复**。

我们相信，经过这些理论解释的强化、存储预算的显式对比以及工程上的加速优化，该方法在实际 CIL 部署场景中的可用性将得到极大增强。非常感谢您极具建设性的审稿意见！

**尊敬的审稿人 (Reviewer Dy6q)，**

*(温馨提示：为方便查阅，我们在本回复中涉及的图表已统一放置于匿名链接中：[https://anonymous.4open.science/r/ICML2026-Rebuttal-D2D6/](https://www.google.com/search?q=https://anonymous.4open.science/r/ICML2026-Rebuttal-D2D6/) 。)*

我们非常感谢您对本论文的详细阅读和建设性评价，特别是您对我们研究动机的认可，以及对方法概念清晰度、原创性和实验全面性的积极评价。针对您在算法机制细节、实际效率以及潜在扩展性方面提出的疑问，我们进行了补充实验和深入分析，具体回复如下：

### 1 关于层次化初始化 (Hierarchical Initialization) 的补充说明

非常抱歉，由于正文篇幅的限制，我们未能详尽展开该部分的背景知识，我们将按照您的建议在最终版本的附录中补充这些细节。

- **方法简述与鲁棒性解释：** 在我们的方法中，凝聚层次聚类（Agglomerative Hierarchical Clustering）使用的是 UPGMA（平均链接）标准与余弦距离度量。相比于纯在线的增量聚类（如原始 SOINN，高度依赖数据输入顺序且易受局部噪声干扰），HC 在任务结束时基于整个特征缓冲区进行一次全局的自底向上聚合。这种方式是**顺序无关的（Order-independent）**，且平均链接机制能有效抑制离群点（Outliers），从而提供了一个全局最优且高度稳定的流形“骨架”。
- **初始特征对后续流程的影响：** SOINN 在构建图时对初始状态非常敏感。如果初始化包含大量由于噪声或乱序产生的虚假节点，后续的竞争性学习（CHL）会浪费大量计算资源去维护无用连接，甚至导致拓扑结构扭曲。HC 提供的鲁棒初始化确保了后续的 SLERP 更新始终围绕着真实的流形高密度区域进行，极大地提升了拓扑精炼的稳定性和最终的分类精度。

### 2 关于球形 SOINN (Spherical SOINN) 与 SLERP 的内在机制

同样地，对于这部分细节的省略我们深表歉意，这部分直观的物理解释将被增补至论文中。

- **SLERP 的必要性：** 在标准的 SOINN 中，节点通过线性插值进行更新。但在我们的归一化特征空间（单位超球体）中，简单的线性插值会将节点拉入超球体的内部，破坏基于余弦相似度的度量空间。SLERP（球面线性插值）严格约束节点沿着流形表面（球面）移动，确保几何度量的一致性。
- **连接 Top-2 节点与邻域更新的直观意义：** 将特征空间中距离输入信号最近的两个节点（Winner and Runner-up）相连，是为了在它们之间建立“局部空间连通性”。这实际上是在动态描绘数据分布的高密度区域轮廓。当获胜节点及其拓扑邻居共同通过 SLERP 更新时，整个高密度区域得以“协同演化（Co-evolve）”。
- **图结构如何辅助分类：** 这种节点与边的组合（图结构）形成了一个紧密贴合复杂非凸流形的网格。它防止了单点原型的“一刀切”线性划分，使得我们在计算局部视角得分（Local View）时，能够精确度量样本是否落入该类别的真实高密度流形区域内，从而显著提升分类的非线性表达能力。

### 3 关于实际效率与超参数调优的担忧

您的担忧非常合理，这是一个关乎模型落地的核心问题。为了避免回复内容的冗余，我们将结合对其他审稿人的详细回复向您进行简要说明：

- **推理延迟的深度优化：** 详细的算法与代码优化细节请您直接参考我们对审稿人 y6R5 （针对缺点 1）的回复。通过引入“两阶段由粗到细检索”机制等深度优化，我们已将推理开销压缩至极低水平，目前优化后的 HC-SOINN 推理速度已大幅超越同为非线性分类器的 KAC (CVPR 2025)，以极小的延迟代价换取了显著的精度提升。
- **超参数与实际可用性：** 关于模型在实际部署中的调参便利性，**请您直接参考我们对审稿人 y6R5 “针对缺点 2 的回复：超参数与实际可用性”部分的详细探讨**。我们在此郑重澄清，我们的方法在实际应用中是**几乎免调参（Tuning-free）的。我们自始至终使用的是完全相同的一套超参数**（例如 $\alpha=0.5, K_{init}=60, age_{max}=20$ 等）。我们曾在内部实验中尝试微调某些参数（如动量系数），发现虽能带来微小的性能增益，但这套固定参数已经足够在绝大多数场景下展现出高度的鲁棒性。性能的飞跃主要源于拓扑架构本身对流形的刻画，而非对个别参数的精雕细琢。

### 4 关于在分类中利用完整图拓扑结构的信息

您的建议非常有意义。在当前的框架中，我们在公式 (7) 的得分计算中仅使用了节点自身的距离信息，图拓扑的边目前确实主要服务于训练阶段的邻域协同更新。

**我们目前未采纳利用完整图拓扑进行推理分类的原因在于：** 充分利用图拓扑信息（例如，引入轻量级图神经网络 GNN 聚合邻居信息，或采用复杂的图遍历与中心性算法评估）不可避免地会带来显著的额外计算开销与内存负担。正如您在问题 3 中所关切的，CIL 场景对实际部署的推理延迟高度敏感。我们目前的“双视角距离计算”正是为了在“分类精度”与“推理效率”之间取得最佳的平衡，以轻量级的方式实现优异的性能。

然而，充分利用拓扑信息在理论上绝对有可能进一步增强模型对局部噪声的抵抗力。受您的启发，我们将把“利用图拓扑进行推理期置信度校准”作为一个极具潜力的未来研究方向（Future Work）在论文结论部分进行探讨。非常感谢您的启发！


### 5. 关于整合原始分类头（FC层）信息的探讨

这是一个非常有洞见的建议。我们在设计初期曾担忧，在严重的灾难性遗忘情境下，原始参数化分类器（FC 层）极易产生“任务近期偏见（Task-recency bias）”，从而干扰拓扑分类的结果。

为了验证您的猜想并探索融合的边界，我们在 DualPrompt + HC-SOINN 架构上补充了集成实验，构建了动态得分融合机制：$\text{Result} = (1-w) \times \text{HC-SOINN} + w \times (\text{Calibrated FC})$，并在三个数据集上均进行了评估。

如匿名链接中的 **Table-3** 所示，实验结果非常有趣且符合直觉：
* **适度融合有益（$w=0.3$）：** 当赋予 FC 层较小权重时，确实带来了性能的提升（例如在 Split CIFAR-100 上，$A_{Last}$ 从无融合的 85.63% 提升至 86.14%）。这说明经过校准的 FC 层依然蕴含着补充性鉴别知识，能与我们的拓扑几何特征形成良好的互补。
* **过度依赖有害（$w=0.5$）：** 当我们进一步提升 FC 层的权重时，整体精度反而出现了明显的下降（例如 Split CUB-200 的 $A_{Last}$ 降至 85.24%，甚至低于无融合状态）。这进一步印证了我们最初的担忧：过多的 FC 参与会重新引入严重的灾难性遗忘和任务偏见，从而掩盖拓扑架构本身的抗遗忘优势。

综上所述，适度引入 FC 层的知识确实有助于分类，但权重不宜过高。非常感谢您的宝贵建议，我们将在修订版的正文或附录中增加对这一融合策略及其权重敏感性的深入讨论。
